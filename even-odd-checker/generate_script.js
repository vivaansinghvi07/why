document.addEventListener("DOMContentLoaded", () => {
	// make it listen for some parameters and:
		// make an input thing spawn the code for copy paste (no editing) 
		// have options to download the code
})
